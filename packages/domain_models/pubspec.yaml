name: domain_models
description: A starting point for Dart libraries or applications.
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  intl: ^0.20.1

dev_dependencies:
  lints: ^5.1.1
  test: ^1.24.0